spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springbite_client_db
    username: postgres
    password: database.postgreSQL00?
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql